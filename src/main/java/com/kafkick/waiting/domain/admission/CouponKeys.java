package com.kafkick.waiting.domain.admission;

/**
 * 쿠폰별 표를 들고 있는 자리들이 담을 수 있는 쿠폰 수.
 *
 * <p><b>쿠폰 식별자는 밖에서 오는 값이라 가짓수에 상한이 없다.</b> 안 막으면 맵
 * 하나가 힙을 밀어내고, 그때 죽는 것은 그 맵이 아니라 노드다.
 *
 * <p>여기 한 곳에서만 정한다. 자리마다 적으면 사본이 갈라지고, 그때 리미터와
 * 격벽이 서로 다른 상한을 쓴다 — 한쪽만 찬 상태에서 판정이 어긋난다.
 */
public final class CouponKeys {

    /**
     * 쿠폰 2,000개를 상정한 값.
     *
     * <p>넘었을 때 무엇을 하는지는 자리마다 다르다 — 래치는 통째로 비우고(판정이
     * 한 틱 헐거워질 뿐이다), 격벽과 리미터는 새 쿠폰을 안 받는다.
     */
    public static final int MAX = 10_000;

    private CouponKeys() {
    }
}
