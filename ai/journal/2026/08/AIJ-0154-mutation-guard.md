---
id: AIJ-0154
date: 2026-08-30
kind: fix
phase: 8
plan: []
jira: CY-793
commits: []
agent: claude-opus-5
confidence: high
promoted-to:
---

# 뮤테이션 가드가 유실이 아닌 것을 유실로 봤다

## 무엇을

미니언이 비정상 종료한 횟수를 세어 실패시키던 가드를, 만든 뮤턴트 수와 리포트에
기록된 수를 맞춰 보고 판정이 안 붙은 뮤턴트가 있는지 세는 것으로 바꿨다.
같이 올렸던 시한 20초는 원래대로 되돌렸다.

## 왜 (근거)

v0.4.0 을 main 에 올린 판에서 `test-mutation` 이 떨어졌다. 점수는 통과선 위였고
가드가 잡은 것이었다 — 미니언 일곱 개가 시간을 넘겨 죽었다.

처음에는 러너가 느려 시한이 모자란 것으로 보고 45초로 올렸다. **틀렸다.** 다른
부하가 전혀 없는 판에서 45초로도 여섯 건이 그대로 났다. 미니언마다 별도 JVM 이라
컨테이너를 띄우는 시험이 그 안에서 다시 뜨고, 그것만으로 수십 초가 간다. 시한으로
없앨 수 있는 종류가 아니다. 그리고 시한을 올린 대가는 컸다 — 실행이 14분에서
24분으로 늘었다.

더 중요한 것은 **미니언이 죽는 것이 유실이 아니라는 사실**이다. 리포트를 직접
세어 확인했다.

| 무엇 | 값 |
|---|---|
| 요약이 말한 수 | 2109 |
| XML 에 기록된 수 | 2109 |
| KILLED | 1891 |
| TIMED_OUT | 6 |
| 요약의 "Killed" | 1897 |

죽은 미니언 여섯과 `TIMED_OUT` 여섯이 정확히 맞고, 그 여섯은 죽인 것으로 세어져
있다. PIT 는 시간을 넘긴 뮤턴트를 판정된 것으로 기록한다. 가드가 잡던 것은
유실이 아니라 **느린 시험**이었다.

## 고려했으나 택하지 않은 것

- **컨테이너를 띄우는 시험을 PIT 시험 집합에서 뺀다** — 앞서 재 본 기록에 따르면
  그렇게 하면 뮤테이션이 91%에서 85%로 떨어진다. 그 시험들이 실제로 뮤턴트를
  죽이고 있다. 느리다고 빼면 게이트가 지키던 것을 잃는다.
- **가드를 지운다** — 비율이 무엇 위에서 나왔는지 아무도 안 보게 된다. 가드의
  목적은 옳았고 재는 대상만 틀렸다.
- **시간 초과를 경고로 남긴다** — 여섯 건은 정상 범위다. 매번 경고가 뜨면 그
  경고를 아무도 안 읽는다.

## 확신이 낮은 부분 / 남은 위험

- 시간을 넘긴 뮤턴트는 죽인 것으로 세어진다. 살아남았을 뮤턴트가 느려서 죽은
  것으로 집계되면 점수가 그만큼 부풀려진다. 2109 중 6 이라 0.3%다. 이 비율이
  크게 늘면 다시 본다.
- 러너와 로컬의 속도 차가 커지면 시간 초과 건수도 는다. 지금은 CI 일곱, 로컬
  여섯으로 비슷하다.

## 어떻게 검증했는가

가드를 워크플로에서 떼어 내 일곱 갈래에 각각 돌렸다. 통과만 확인하면 가드가
아무것도 안 잡아도 초록이다.

| 넣은 것 | 나온 것 | 종료 코드 |
|---|---|---|
| 실제 산출물 | 통과 | 0 |
| 실제 산출물 + 그레이들 137 | 통과 뒤 그레이들 코드 전달 | 137 |
| 요약 줄 없음 | 안내 | 1 |
| 요약 줄 없음 + 그레이들 137 | 안내 + 코드 보존 | 137 |
| 리포트 없음 | 안내 | 1 |
| 리포트 절단 (1198/2109) | 개수 불일치 | 1 |
| 따옴표 형식 변경 | 상태를 못 읽었다 | 1 |
| RUN_ERROR 1891 건 주입 | 판정 안 붙음 | 1 |

## 리뷰가 잡은 것

가드를 고치면서 **가드 자신이 같은 병에 걸렸다.** 리뷰가 실제로 주입해 보고
잡았다.

- `set -o pipefail` 아래에서 `grep` 이 0건이면 대입 자체가 실패한다. `set -e` 가
  그 자리에서 셸을 죽여, "로그 형식이 바뀌었다" 는 안내가 **한 줄도 안 나온다.**
  그레이들의 종료 코드(예: OOM 킬 137)도 1 로 뭉개진다. 빨간데 왜 빨간지 말할
  수 없는 상태였다.
- 판정 상태를 세는 검사에 자기검증이 없었다. 리포트의 따옴표가 겹따옴표로
  바뀌면 검사가 0건을 내는데, 그 0 은 "판정이 다 붙었다" 가 아니라 "아무것도
  못 읽었다" 다. 실제로 2109 중 2064 개가 RUN_ERROR 인 리포트가 초록으로
  통과했다.
- 그 검사의 변수가 비면 `[ "" -gt 0 ]` 이 오류를 내면서도 거짓으로 떨어져
  **통과한다.** 방향이 반대다. 못 셌으면 실패해야 한다.

파이프라인마다 `|| true` 를 붙이고, 종료 코드를 보존하는 `die` 를 두고, 읽은
상태 수가 뮤턴트 수와 같은지 먼저 확인하고, 셈이 숫자가 아니면 실패하게 했다.

## 다음 사람이 알아야 할 것

**`Minion exited abnormally` 를 유실 신호로 읽지 않는다.** 유실인지 아는 방법은
`Generated N` 과 `mutations.xml` 의 `<mutation` 개수를 맞춰 보는 것이다. 둘이
같으면 잃은 것이 없다.

이 저장소의 뮤테이션은 `main` push 파이프라인에서만 돈다. PR 에는 없다.
릴리스를 머지한 뒤 그 파이프라인을 확인하지 않으면 이번처럼 며칠을 빨간 채로
지나간다.
