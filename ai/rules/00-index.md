# 규칙 색인

각 규칙에는 **ID**가 있다. 예외를 둘 때 이 ID로 참조한다.

```java
// RULE-EXCEPTION(JS-1): Redis Lua 스크립트 문자열에 포함된 클래스명이라 import 불가
```

예외 주석을 다는 위치와 규칙은 [`../README.md`](../README.md) 에 있다.

---

## 문서

| 문서 | 접두사 | 범위 |
|---|---|---|
| [10-java-style.md](10-java-style.md) | `JS` | 문법·표기·Lombok·Javadoc |
| [20-design.md](20-design.md) | `DS` | DDD·SOLID·디자인 패턴·계층 |
| [30-reactive.md](30-reactive.md) | `RX` | WebFlux·Reactor |
| [40-testing.md](40-testing.md) | `TS` | TDD·픽스처·커버리지 |
| [50-redis.md](50-redis.md) | `RD` | 키 스킴·Lua·클러스터 |
| [60-workflow.md](60-workflow.md) | `WF` | 커밋·Jira·문서·작업 로그 |
| [70-packages.md](70-packages.md) | `PK` | 패키지별 추가 규칙 |
| [80-logging.md](80-logging.md) | `LG` | 로깅·Loki·관측 |
| [90-errors.md](90-errors.md) | `EX` | 예외 계층·에러 코드·전역 처리 |

---

## 강제 수준

| 수준 | 의미 | 어겼을 때 |
|---|---|---|
| **MUST** | 예외 없음 | 훅이 차단. 우회 금지 |
| **SHOULD** | 근거 있으면 예외 가능 | `RULE-EXCEPTION` 주석 + journal 기록 |
| **PREFER** | 판단에 맡김 | 리뷰에서 논의 |

훅이 차단하는 것은 MUST 중 **기계적으로 판정 가능한 것**뿐이다.

**"훅 차단" 열에 거짓을 적지 않는다.** 차단한다고 적어두면 아무도 그 규칙을
사람 리뷰에서 보지 않는다 — 없는 검사를 믿는 것이 검사가 없는 것보다 나쁘다.
아직 없으면 언제 생기는지를 적는다.
나머지는 리뷰 에이전트와 사람이 본다.

| 표기 | 무엇이 막는가 | 언제 |
|---|---|---|
| ✅ | `check-java.sh`·`check-lua.sh` | 파일을 쓰는 순간 |
| **PR** | `review-branch.sh` → `guard-pr.sh` | `gh pr create` 시점 |
| CI | 워크플로 | 푸시 이후 |

> **✅ 는 `Write`·`Edit` 로 쓴 파일만 본다.** 힙독이나 스크립트로 만든 파일은
> 그 훅을 지나가므로, 브랜치 전체를 다시 보는 **PR** 단이 필요하다
> ([AIJ-0013](../journal/2026/08/AIJ-0013-local-review-before-pr.md)).

---

## MUST 목록 (전체)

한눈에 보기 위한 사본이 아니라 **색인**이다. 내용은 각 문서에 있다.

| ID | 규칙 | 훅 차단 |
|---|---|---|
| JS-1 | FQDN 금지 — import 해서 쓴다 | ✅ |
| JS-2 | 와일드카드 import 금지 | ✅ |
| JS-6 | Javadoc 5줄 이하 | ✅ |
| JS-9 | `@Data` 금지 | ✅ |
| JS-11 | 식별자는 영문. 시험은 타입 이름만 (TS-2 가 시험 이름·헬퍼를 한글로) | ✅ |
| JS-11 | 빌드 스크립트 선언 이름도 영문 | **PR**·CI |
| JS-12 | 생성자 대신 정적 팩토리 | **PR** |
| JS-13 | `private static` 메서드 금지 | ✅ |
| JS-14 | 유틸리티·중첩 클래스는 `static` | ✅ |
| DS-1 | 도메인은 Spring·Redis·시계를 참조하지 않는다 | ✅ |
| DS-2 | 도달 불가 상태를 만들 수 있는 public 생성자 금지 | — |
| DS-8 | 직접 만들기 전에 라이브러리부터 찾는다. 판은 실측해 고른다 | — |
| RX-1 | 블로킹 호출 금지 | ✅ |
| RX-4 | `subscribe()` 결과를 버리지 않는다 | — |
| TS-1 | 구현보다 테스트를 먼저 커밋한다 | — |
| TS-3 | 픽스처는 도달 가능한 상태만 만든다 | — |
| RD-1 | Lua는 KEYS에 없는 키를 만지지 않는다 | ✅ |
| RD-4 | 요청 경로에서 Redis를 치지 않는다 | — |
| RD-10 | Lua 상단에 KEYS/ARGV 계약 주석 | ✅ |
| WF-1 | Conventional Commits 형식 | ✅ |
| WF-5 | `waiting-legacy/` 수정 금지 | ✅ |
| WF-6 | 코드 변경 세션은 journal 엔트리를 남긴다 | 알림 |
| LG-1 | 요청당 로그 금지 — 세는 것은 메트릭 | — |
| LG-2 | 상태 전이는 진입·해제를 쌍으로 로깅 | — |
| LG-4 | Loki 라벨은 저카디널리티만 | 6.5.2 예정 |
| LG-5 | 리액티브에서 MDC 금지 | ✅ |
| LG-6 | 개인정보·비밀 로깅 금지 | ✅ |
| EX-1 | 정상 실패는 예외가 아니다 (판정값으로) | **PR** |
| EX-2 | 모든 예외는 `WaitingException` 상속 | — |
| EX-7 | 내부 정보를 응답에 담지 않는다 | — |
| EX-12 | 전역 처리는 `ErrorWebExceptionHandler` | — |
| JS-5 | 필드는 `final` | — |
| RX-2 | 주기 루프는 `repeatWhen`, `Flux.interval` 아님 | **PR** |
| RX-3 | 배경 루프에 스케줄러를 명시한다 | — |
| RX-5 | 에러가 루프를 죽이지 않게 한다 | — |
| RX-6 | 실패해도 마지막 좋은 상태를 지운다 | — |
| RX-7 | 요청 바디를 읽지 않는다 | — |
| RX-11 | 공유 가변 상태에는 메모리 가시성을 명시한다 | — |
| TS-2 | 테스트 이름은 한글 문장 | — |
| TS-4 | 시계를 고정한다 | **PR** |
| TS-7 | 불안정 테스트는 격리하지 말고 고친다 | **PR** |
| TS-11 | 약한 단언을 쓰지 않는다 | **PR** |
| TS-8 | 카오스도 TDD로 쓴다 | — |
| TS-9 | 하네스를 자기검증한다 | CI |
| RD-2 | 한 스크립트는 한 슬롯만 만진다 | — |
| RD-3 | 키 이름은 한 곳에서만 만든다 | — |
| RD-5 | TTL을 신선도로 착각하지 않는다 | — |
| RD-6 | 키스페이스 알림에 의존하지 않는다 | — |
| RD-7 | 무제한으로 자라는 키를 만들지 않는다 | — |
| RD-8 | 큐 score 의 단조성을 보장한다 | — |
| WF-2 | TDD 사이클을 커밋으로 남긴다 | ✅ |
| WF-3 | git flow + Jira 티켓 ID 브랜치 | CI |
| WF-4 | 게이트 커밋에 실측치를 남긴다 | — |
| WF-7 | 문서는 늘릴 때 함께 줄인다 | — |
| LG-3 | 반복 로그를 억제한다 | — |
| LG-7 | 레벨의 의미를 지킨다 | — |
| EX-3 | `BusinessException`은 스택트레이스를 채우지 않는다 | — |
| EX-4 | 검사 예외를 만들지 않는다 | — |
| EX-5 | 에러 코드는 enum 한 곳에서 관리한다 | — |
| EX-6 | 응답 규격을 백엔드와 일치시킨다 | — |
| EX-8 | 체인 안에서 `throw` 하지 않는다 | — |
| EX-9 | 경계에서 번역한다 | — |
| EX-10 | 예외를 삼키지 않는다 | — |
| EX-11 | 응답이 커밋된 뒤에는 되돌리지 않는다 | — |
| EX-13 | 알 수 없는 예외는 500으로 수렴시키되 로그를 남긴다 | — |
| PK-0 | 패키지 루트는 `com.kafkick.waiting` | 2.8.1 |
