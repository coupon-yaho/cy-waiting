---
id: AIJ-0165
date: 2026-08-31
kind: fix
phase: 8
plan: []
jira: CY-830
commits: [bb848e4, 9ab1321, HEAD]
agent: claude-opus-5
confidence: high
promoted-to:
---

# 멱등 키 형식이 뒷단 계약과 안 맞았다

## 무엇을

클라이언트가 준 UUID 를 그대로 넘긴다. 회원에 묶어 다시 서명하던 것을 뺐다.

## 왜

뒷단은 `Idempotency-Key` 를 UUID 형식으로만 받는다. 게이트웨이는 `ik_` 접두에
HMAC-SHA256 을 base64url 로 찍은 46자를 보내고 있었다. **실제로 붙이면 발급
경로가 통째로 거절된다.**

**아무 시험도 이걸 안 잡았다.** 카오스 시나리오의 뒷단 스텁이 헤더를 안 보고
200 만 돌려주므로 형식이 무엇이든 통과한다. 실제 쿠폰 서비스와 붙여 본 적이
없어 드러나지 않았다. Phase 8 이 "장애 중에도 동작한다" 를 검증하는 동안, 정상
경로가 실제 뒷단과 붙으면 안 돈다는 것을 못 보고 있었다.

## C-11 은 그대로다

시도를 가르는 것은 여전히 클라이언트다. 입장 토큰을 재료로 못 쓰는 이유도
그대로다 — 30초 창마다 바뀌고 통과 경로마다 갈린다.

바뀐 것은 **도용 방어를 누가 지는가** 다. 게이트웨이가 회원에 묶어 서명하던
것을, 뒷단이 회원과 키의 쌍으로 저장해서 진다. 남의 키를 실어도 자기 회원
앞으로만 쓰이므로 남의 시도를 못 지운다.

## 덤으로 풀린 것

파생이 비밀키에 묶여 있던 문제가 사라졌다 (CY-829). 비밀키를 회전하면 진행
중이던 재시도의 키가 통째로 바뀌어 이중 발급이 났는데, 이제 회전과 무관하다.
게이트웨이를 우회하는 진입점이 생겨도 같은 키가 가고, 프론트가 아는 값과 뒷단이
본 값이 같아 사고 때 대조도 된다.

## 남긴 갈래

**UUID 가 아니면 안 준 것으로 본다.** 그대로 넘기면 뒷단이 형식으로 거절해
사용자가 발급을 못 받는다. 게이트웨이가 뒷단 계약을 아는 유일한 자리다.

**대소문자를 맞춘다.** 같은 값을 표기만 다르게 재시도하면 뒷단이 두 건으로 본다.

**값을 안 주면 쿠폰과 회원으로 떨어진다** (C-11 그대로). 그 자리도 UUID 로 낸다.
비밀키를 안 쓰므로 추측 가능하지만, 뒷단이 회원으로 묶으므로 남의 시도를 못
건드린다.

## 절차

**핫픽스로 냈다.** 사용자 지시로 에이전트 리뷰를 건너뛰고 main 으로 바로 갔다.
빌드·전 계층 시험·기계 검사는 돌렸다 — 깨진 것을 올리는 것은 빠른 것이 아니다.

## 뒷단이 두 가지를 더 짚었다

**폴백이 v3 이었다.** `UUID.nameUUIDFromBytes` 는 MD5 기반이라 판 자리가 3 이다.
뒷단 계약이 v4 라 거절당한다. 해시를 SHA-256 으로 바꾸고 판·변종 자리를 직접
세웠다 — 같은 재료에 같은 값이 나오는 성질은 그대로다.

**검증이 판 자리를 안 봤다.** 모양만 맞으면 v1·v3 도 그대로 넘어가 뒷단에서
죽었다. 정규식이 판 자리와 변종 자리까지 본다. 아니면 안 준 것으로 보고
떨어진다 — 400 으로 막으면 v1 을 쓰는 정상 클라이언트가 발급을 아예 못 받는데,
그건 "잃는 쪽" 이 아니라 "못 받는 쪽" 이라 더 나쁘다.

## 로케일이 결정성을 깨고 있었다

로컬 리뷰가 잠복해 있던 것을 찾았다. `String.formatted` 는 기본 로케일을 쓰므로
`%d` 가 그 로케일의 숫자로 찍힌다. 힌디나 아랍 숫자 로케일에서는 재료 바이트가
달라지고, **같은 사람에게 다른 키가 나온다.**

노드마다 로케일이 다르거나 재기동하며 환경이 바뀌면 재시도가 두 키로 갈라져
멱등성이 안 걸린다. C-11 이 받아들인 "잃는 쪽" 이 아니라 반대쪽 — **이중 발급**
이다. 컨테이너가 로케일을 안 박으므로 환경에서 온다.

**이 줄은 이번 변경이 만든 것이 아니다.** 다만 앞 판에서는 폴백이 v3 이라 뒷단이
전부 거절했고, 그래서 그 경로로는 발급이 아예 안 났다. 이 핫픽스가 폴백을 처음
살리는 변경이라 그때부터 실제 사고가 된다.

`Locale.ROOT` 를 박았다.

## 시험이 자기끼리만 비교하고 있었다

일곱 시험이 전부 같은 JVM 안에서 `keys.of(...)` 끼리 비교한다. 자기 자신과의
비교라 재료가 바뀌어도 로케일이 바뀌어도 다 통과한다 — 위 로케일 결함을 못
잡았다. 값을 박아야 "재기동해도 같은 값" 이 못 박힌다.

로케일을 바꿔 놓고 같은 값이 나오는지 보는 시험도 같이 뒀다.

## 남긴 것

폴백으로 떨어진 것을 세는 신호가 없다 (CY-831). 조용히 발급을 잃는 경로인데
이번에 입구가 v1·v3·변종 불일치까지 넓어졌다.

계약을 못 박는 시험이 없다. 뒷단 스텁이 헤더를 검사하게 하면 형식 회귀가
그 자리에서 잡힌다. 이번 핫픽스 범위 밖이라 CY-830 에 남긴다.
