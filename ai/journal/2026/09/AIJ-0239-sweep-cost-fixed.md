---
id: AIJ-0239
date: 2026-09-05
kind: fix
plan: [10-scale-out 10.0.5]
phase: 10
jira: CY-922
commits: [HEAD]
agent: claude-opus-5
confidence: high
promoted-to:
---

# 앞줄을 안 걷는 회차가 앞줄을 읽고 있었다

## 무엇을

[AIJ-0237](AIJ-0237-sweep-costs-more.md) 이 순서를 매긴 넷 중 둘을 고쳤고,
**하나는 실측이 반증해서 안 했다.**

## 왜 (근거)

`sweep.lua` 는 앞줄 제거를 접고 도는 회차가 있다. 리더 승계 유예 구간이 그
자리고, 그때는 **모든 쿠폰**에 대해 접고 돈다. 그런데 창을 읽는 두 명령이
그 조건과 무관하게 항상 돌았다 — 접는 값은 그 아래 제거 루프의 상한에만
걸려 있었다.

그리고 목록을 `t[#t + 1] = v` 로 쌓고 있었다. Lua 5.1 의 길이 연산자는 매번
이진 탐색을 돌아 누적이 O(n log n) 이 된다.

## 어떻게 검증했는가

**RED 를 먼저 세웠다.** 명령별 호출 수를 되돌린 뒤 세서, 접은 회차는 창을
읽는 두 명령이 0 이어야 하고 안 접은 회차는 0 이 아니어야 한다. 둘을 같이
둔 이유는 늘 0 을 내는 구현이 통과하지 못하게 하기 위해서다. 첫 시험이 정확히
실패하고 대조 시험은 통과했다.

고치기 전과 뒤를 같은 하네스로 쟀다 (큐 20,000 · K=3000 · 다섯 번 평균).

| 회차 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 앞줄을 걷는다 | 1,274μs | 1,266μs |
| **앞줄을 접는다** | **1,494μs** | **64.7μs** |

접은 회차가 23 분의 1 이다. 걷는 회차는 하는 일이 같으니 그대로다.

승계 유예는 `aliveTtl + maxInterval` 만큼, 즉 약 290 틱이다. 그동안 쿠폰마다
매 틱 1.4ms 를 버렸다 — 쿠폰 스무 개면 틱당 30ms 다.

**길이 연산자 쪽도 따로 쟀다.** 원소 3,000 개를 쌓는 데 649μs 대 141μs 다.
1,000 개에서 152 대 104, 10,000 개에서 1,262 대 434 로 격차가 초선형으로
벌어진다.

## 리뷰가 찾은 같은 자리 하나 더

**제거 루프는 `removeFront` 만 보는 게 아니다.** 살아 있는 신호가 하나도 없으면
그때도 아무도 안 걷는다. 그런데 나는 읽기 조건에 `removeFront` 만 접었다.

| 회차 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 생존 신호가 비었다 | 999μs | **107μs** |
| 생존 신호가 전부 만료 | 2,541μs | **322μs** |
| 정상 (전부 생존) | 950μs | 860μs |

**이쪽이 승계 유예보다 나쁘다.** 유예는 `SweepGate` 가 끝내 주는데 "저장소를
잃은" 상태는 끝내 주는 것이 없다. 뒤처진 복제본 승격이나 AOF 유실 뒤에는 매
틱 되풀이된다.

접는 회차에서는 생존 여부를 묻지도 않게 했다. `and` 가 왼쪽부터 끊으므로
`ZCOUNT` 자체가 안 돈다 — 그 값을 아무도 안 쓰는 회차다.

## 내 시험이 비용만 쟀다

리뷰가 **접은 회차가 아무것도 안 하고 곧장 돌아가는 구현**을 만들어 돌려 봤다.
내 시험 둘이 그것을 통과시킨다 — 두 명령이 0 이고 대조군은 그대로니까.

그런데 그 상태는 스크립트가 "절대 안 된다" 고 못 박은 것이다. 접는 것은 앞줄
제거뿐이고 만료 신호와 낡은 기록은 걷어야 한다. 안 걷으면 승계 유예 내내
해시가 한 방향으로만 자라고 커서가 전진을 못 한다.

**비용만 재는 시험은 "안 했다" 와 "덜 했다" 를 못 가른다.** 반환값 셋과 실제
키 상태를 같이 못 박았고, 그 구현이 이제 죽는 것을 확인했다.

**계측이 다른 시험에 노출돼 있었다.** 명령 계수는 서버 전역인데 스케줄러가
켜진 통합 시험 둘의 스프링 컨텍스트가 캐시돼 계속 돈다. 그 배분 틱이 측정
창에 끼어들면 이유 없이 빨개진다. 이 회차가 스크립트 하나만 돌렸는지 같이
봐서, 그때는 "측정이 오염됐다" 로 먼저 터지게 했다.

## 반증된 것

**"뜨거운 쿠폰의 스캔 폭이 한산한 쿠폰에 전염된다" 는 틀렸다.**

`AIJ-0237` 은 스캔 상한을 대상 중 가장 넓은 크레딧 하나로 정해 모든 쿠폰에
쓰므로, 대기자 셋짜리 쿠폰도 3,000 폭으로 훑는다고 적었다. 재 보니 아니다.

| 줄 길이 | K=10 | K=3000 |
|---|---|---|
| 3 명 | 1.60μs | **1.47μs** |
| 50 명 | 3.16μs | 11.45μs |

`LIMIT` 은 **돌려주는 개수**를 자르고, 그 수는 범위 안 원소 수로 이미 막혀
있다. 대기자 셋이면 K 가 무엇이든 셋만 돌려주고 비용도 같다. 50 명 쪽의
차이도 K 탓이 아니라 10 개 대 50 개를 돌려준 차이다.

**포트 시그니처를 바꿔 시험 다섯을 건드릴 뻔했다.** 감사의 지적이 그럴듯해도
재 보기 전에는 손대지 않는다.

## 같은 패턴이 있지만 안 고친 곳

`snapshot_publish.lua` 와 `gateway_heartbeat.lua` 도 `t[#t + 1]` 로 쌓는다.
**거기는 안 고쳤다** — 재 본 격차가 원소 수천 개에서 나오는데 그 목록들은
그만큼 안 커진다.

- `snapshot_publish` 의 `chunk` 는 512 에서 비워지고, `stale` 은 끝난 쿠폰
  수만큼이다. 그리고 `#chunk == CHUNK` 는 읽기라 뜻이 있다
- `gateway_heartbeat` 의 `ids`·`votes`·`dead` 는 게이트웨이 대수만큼이다

n=1,000 에서 격차가 48μs 이고 그 아래로는 더 작다. 잰 이득이 없는 곳을 고치면
위험만 는다.

## 고려했으나 택하지 않은 것

- **`WITHSCORES` 제거.** 실측 절감이 크지만(원소당 정확히 두 배) 그 rank 가
  임계가 문자열을 거치며 접힐 때 되잡는 수단이다. 대체 수단을 같이 넣어야
  하므로 별도 작업으로 둔다
- **`sweep` 을 alive 주도로 뒤집기.** 비용이 K 가 아니라 실제 이탈자 수에
  비례하게 되지만 설계 변경이다

## 확신이 낮은 부분 / 남은 위험

**걷는 회차는 그대로다.** 1,266μs 는 여전히 등록 40 건 분량이고, 그 회차가
정상 구간의 대부분이다. 위의 둘 중 하나를 해야 그쪽이 준다.

## 다음 사람이 알아야 할 것

**센 값과 자른 목록이 갈릴 수 있다.** 누적을 세는 변수로 바꿀 때, 예산으로
목록을 자르는 자리에서 그 값이 안 맞았다 — 잘린 회차가 자르기 전 수를 지운
것으로 보고했다. 기존 시험이 그것을 잡았다. 자른 뒤 다시 센다.

**감사 결과도 실측으로 검증한다.** 넷 중 하나가 틀렸다. 그럴듯한 설명과
실제로 그런 것은 다르다 — 이 페이즈가 계속 그것을 배우고 있다.
