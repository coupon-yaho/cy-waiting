---
id: AIJ-0172
date: 2026-08-31
kind: implement
phase: 8
plan: [08-resilience 8.3.4]
jira: CY-842
commits: [a322458, 63e9dd3, HEAD]
agent: claude-opus-5
confidence: medium
promoted-to:
---

# C12 — 레디스를 kill -9 하고 영속으로 되살린다

## C1 과 무엇이 다른가

죽이는 방법은 같다. 갈리는 것은 **돌아온 뒤에 무엇이 남아 있는가**다. C1 은
통째로 비어 있고 여기는 마지막 창만 없다.

그런데 픽스처가 `appendonly no` 로 컨테이너를 띄우고 있었다. 그 상태로 C12 를
쓰면 통째로 날아간 판을 재게 되고, 그건 C1 을 두 번 재는 것이다. 영속을 켜는
갈래를 더하고 **픽스처 자기검증을 함께 넣었다** — 켜면 남고 끄면 사라진다.
둘이 안 갈리면 이 시나리오의 전제가 없다.

## 증발이 안 일어나고 있었다

내려쓴 뒤에 등록하고 곧바로 죽였다. 열에 열이 살아남았다 — everysec 이 이미
내려쓴 뒤였다. 그러면 회복 구간의 재등록이 **재조회**가 되어, 추월 판정이
원래 자리를 다시 읽는 것에 지나지 않는다.

덧붙이기를 멈춘 뒤에 등록한다. 그 쓰기는 메모리에만 남고 파일은 그대로라,
다시 켜면 그때까지가 실린다. everysec 창이 통째로 날아간 최악의 판과 같은
모양이고, 무엇보다 **결정적이다.** 이제 열 명이 사라지고 열 명이 남는다.

증발이 실제로 일어났는지도 전제로 걸었다. 다 살아남으면 그 자리에서 떨어진다.

## 유지 구간의 fail-open 은 여기서 안 잰다

레디스가 죽으면 줄에 세울 방법 자체가 없어, F1 의 세 번째 규칙이 적용되는
유일한 구간이다. 그 통과가 상한 안인지는 C1 이 재고 여기서 다시 안 잰다.
처음에는 "추월 0" 을 걸었다가 뺐다 — 열 건이 뒷단까지 갔고, 그건 맞는
동작이었다.

유지 구간에 요구하는 것은 하나다. 판정이 멎지 않는다. 전원 5xx 면 회복 구간의
관측이 무엇을 뜻하는지 알 수 없다.

## 무엇이 죽는가

영속을 끄면 스냅샷이 안 돌아와 회복 구간이 통째로 떨어진다. 재등록자에게 맨
앞자리를 주면 "자리 1.0 을 받아 남은 사람을 앞섰다" 로 잡힌다.

다만 **바닥값 방어만 없애는 것은 안 잡힌다.** 시계가 정상이면 새 점수가 이미
남은 사람보다 크므로 바닥값이 할 일이 없다. 그것이 값을 하는 것은 시계가 뒤로
갔을 때이고, 그 판은 C13 이 만든다 (Phase 10).

## 남긴 것

초과 발급은 못 잰다. 뒷단이 스텁이라 재고를 안 깎는다.

RC1·RC3·RC6 도 안 잰다. 회복까지의 시간 판정도 없다.

증발한 사람이 **자기가 사라진 줄 모르는 채** 폴링하는 경로는 안 본다. 지금은
같은 사람이 다시 등록하는 것만 본다. 폴링이 없는 자리를 어떻게 알려 주는지는
별도로 봐야 한다.
