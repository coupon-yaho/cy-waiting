---
id: AIJ-0071
date: 2026-08-25
kind: implement
phase: 5
plan: [05-data-plane]
jira: CY-404
commits: []
agent: claude-opus-5
confidence: medium
promoted-to:
---

# 사용자 단위 상한이 어디에도 없었다

## 큐가 막는다고 괜찮은 것이 아니다

판정의 상한은 쿠폰별과 노드 전역뿐이다. 한 사람이 노드 예산을 다 먹을 수 있고,
그러면 **정상 사용자가 전부 큐로 밀린다.** 큐가 결국 막긴 하지만 그것 자체가
공격 성공이다 — 무대기 통과라는 요구가 공격자 한 명에게 무너진다.

## 판정 앞에서 끊는다

판정에 들어가면 남용 요청이 노드 예산을 만지고, 큐에 넣으면 공격자가 자리를
차지한다. 그 자리는 정상 사용자의 것이다.

## 회원과 주소를 함께 본다

로그인이 없어 회원 식별자를 바꾸는 비용이 **0** 이다. 그것만으로는 우회된다.
주소가 실질적인 방어고, 회원 상한은 같은 주소 뒤의 여럿을 가른다.

**주소는 맨 끝만 믿는다.** 프록시가 자기 앞의 주소를 뒤에 붙이므로 우리가 아는
홉이 넣은 값은 마지막이다. 앞쪽은 클라이언트가 채워 넣을 수 있어, 그걸 믿으면
남의 주소로 위장해 남의 몫을 태우고 정작 자기는 안 걸린다.

## 상한을 거꾸로 잡고 있었다

처음에 폴링 상한을 발급보다 **좁게** 뒀다. 계획서는 반대를 적어 두고 있다 —
게이트웨이가 1초 간격으로 물으라고 지시해 놓고 그 폴링을 남용으로 막으면 정상
대기자가 끊긴다. 탭이 여럿이면 지시를 지켜도 발급 상한은 쉽게 넘는다.

되돌리기 프로브가 알려 줬다. "폴링도 발급 상한으로" 를 넣었는데 아무 시험도 안
깨졌다 — 그때는 그 변경이 되레 느슨해지는 방향이었기 때문이다.

## 어떻게 검증했는가

| 되돌린 것 | 잡는 시험 |
|---|---|
| 회원 상한 제거 | 한_사람이_너무_빨리_두드리면_막는다 · 어느_키로_막았는지_센다 |
| 주소 상한 제거 | 식별자를_바꿔도_주소로_막는다 외 1건 |
| 앞쪽 주소를 믿음 | 앞에_끼워_넣은_주소를_안_믿는다 |
| 폴링도 발급 상한으로 | 폴링은_발급보다_느슨하다 |
| 막힌 요청도 흘려보냄 | 한_사람이_너무_빨리_두드리면_막는다 외 2건 |

## 부하 시험이 정상 사용자를 막는 것을 잡았다

CI 의 k6 가 통과율 57% 로 떨어졌다. VU 20 이 한 주소를 쓰는데 주소 상한을 사람당
값과 같게 잡아 둔 탓이다.

**이건 하네스 문제가 아니라 설계 문제였다.** NAT 나 회사 프록시 뒤에서는 수백
명이 한 주소를 쓴다. 그 상한이면 그들이 통째로 막히는데, 막으려던 것이 아니라
정상 사용자다. 사람당과 주소당 상한을 갈랐다 — 주소당은 "한 대가 두드리는 것" 을
잡는 값이라 훨씬 커야 한다.

**하네스도 같이 고쳤다.** VU 마다 다른 주소를 주고, 지시한 폴링 간격만큼 쉰다.
게이트웨이가 1초로 물으라고 하는데 하네스가 쉬지 않고 두드리면 남용 방지가 막는
것이 맞고, 그건 잴 값이 아니다. 앞단 프록시가 실제 주소를 붙이는 운영 모양과도
그쪽이 가깝다.

## 남용 방지가 서비스 거부 수단이 될 뻔했다

리뷰가 잡았다. 전달 헤더를 **신뢰 홉 확인 없이** 믿고 있었다.

그러면 클라이언트가 매 요청 다른 값을 넣어 상한을 넘는 것으로 끝나지 않는다.
초당 키 십만 개를 채우면 리미터가 신규 키를 거부하고, **그때부터 정상 사용자가
429 를 받는다.** 막으려던 것으로 막아야 할 사람을 막는다.

소켓 주소가 신뢰 대역일 때만 헤더를 본다. 설정이 비면 아무 헤더도 안 믿는다 —
헤더를 믿는 것은 앞단이 그것을 덮어쓴다는 보장이 있을 때만이다.

**거부된 요청이 앞의 예산을 깎고 있었다.** 회원을 먼저 차감하고 주소를 검사해서,
주소에서 막히면 이미 깎인 회원 몫이 안 돌아온다. 통과한 요청이 하나도 없는데
예산이 빈다. 한 걸음에 둘 다 잡는 함수가 이 문제를 위해 이미 있었다.

**그 시험을 처음엔 못 쟀다.** 시계를 1초 넘겨서 재려 했는데, 넘기면 예산이 전부
풀려 새는 것이 안 보인다. 같은 초 안에서 봐야 한다.

## 대역 표기가 아무 주소도 안 잡았다

신뢰 홉을 접두 문자열로 보고 있었다. `10.0.0.0/8` 은 문자열로는 어디에도 안 붙어서
**설정을 해도 아무도 신뢰되지 않는다.** 검사가 있는데 늘 거짓인 상태다.

비트로 자른다. 바이트 경계가 아닌 대역도 읽고, 못 읽는 표기는 안 믿는다 — 오타
하나가 전 대역을 여는 것보다 낫다. 이름은 안 찾는다. 설정에 호스트명이 들어오면
그 조회가 요청 경로에 붙는다.

**주소를 못 읽는 요청을 그냥 통과시키고 있었다.** 열어 주면 그 상태를 만드는 것이
곧 우회 통로다. 빈 전달 값을 프록시 주소로 바꾸던 것도 같다 — 빈 값을 보내는
것만으로 그 프록시 뒤의 모두를 막을 수 있다.

## 남은 것

**폴링 지시가 늘어도 상한이 안 따라간다** (5.7.8). 전역 예산이 모자라 간격을
늘리면 상한도 함께 내려가야 하는데 지금은 상수다. 그 배수를 읽으려면 스냅샷을
봐야 하고, 이 필터는 판정 앞이라 아직 스냅샷을 안 본다.

**상한값이 실측이 아니다.** 사람이 손으로 누를 수 있는 수의 몇 배로 잡았다.
정상 사용자가 걸리는지는 지표로 본다 — 그래서 사유를 키 종류별로 센다.
