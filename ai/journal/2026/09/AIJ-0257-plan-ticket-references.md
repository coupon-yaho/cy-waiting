---
id: AIJ-0257
date: 2026-09-08
kind: fix
plan: [08-resilience, 09-routing, 90-decisions, HANDOFF]
phase: 9
jira: CY-891
commits: [HEAD]
agent: claude-opus-5
confidence: high
promoted-to:
---

# 계획서가 없는 티켓을 가리키고 있었다

## 무엇을

계획서가 참조하는 티켓 키 217 종을 전부 조회했다. **열다섯 종이 실재하지 않았고**
그 참조가 47 곳이었다. 실재하는데 내용이 전혀 다른 자리도 스무 곳쯤 있었다 —
예를 들어 C15 시나리오를 CY-879 로 적었는데 그 티켓은 계량기 결함이다.

## 왜 지금 더 나빠졌나

[[AIJ-0252]] 가 커밋 푸터의 가짜 키를 고쳤을 때 이 문서들은 안 봤다. 그리고 이번
회차에 800번대 티켓을 새로 쓰면서 **가짜 키가 실재 티켓으로 해석되기 시작했다.**

전에는 링크를 누르면 "없는 이슈" 가 떠서 최소한 틀린 줄은 알았다. 지금은 엉뚱한
티켓이 열리고 읽는 사람이 그것을 그 작업이라고 믿는다. 없는 추적보다 틀린 추적이
나쁘다.

## 어떻게 고쳤나

**못 맞추는 키는 지웠다.** 다른 번호로 바꾸지 않았다 — 어느 티켓이 그 작업이었는지
모르는 상태에서 그럴듯한 번호를 넣는 것이 이 사고를 만든 방식이다. 본문의 실질
내용(무엇이 닫혔고 무엇이 남았는지)은 그대로 남겼다.

**커트라인을 한 번 틀렸다.** 처음에는 "860 이상은 의심" 으로 쓸었는데, 이 프로젝트의
실제 이슈는 `CY-872` 까지다 ([[AIJ-0252]]). 그래서 실재하고 문맥도 맞는 넷
(`CY-860`·`CY-866`·`CY-869`·`CY-871`)을 같이 지웠고 리뷰가 잡았다. 되돌렸다.

남긴 것은 제목이 그 자리의 내용과 실제로 맞는 것들이다 — `CY-821`·`CY-839`·`CY-840`·
`CY-842`·`CY-860`·`CY-862`·`CY-866`·`CY-868`·`CY-869`·`CY-871`, 그리고 이번 회차에
새로 쓴 `CY-875`·`CY-887`·`CY-889`·`CY-890`.

**빈 비고는 저널 ID 로 채웠다.** 키만 있던 표 칸이 "닫힘" 만 남으면 어디서 닫혔는지가
사라진다. 저널은 이 저장소 안에 있어 링크 검사가 오탈자를 잡는다.

## 다시 안 나게

`guard-pr.sh` 가 **고친 문서가 가리키는 키**도 본다. 브랜치 키만 보던 것을 넓혔다.
CI 는 이 검사를 못 한다 — 그쪽 계정이 이 프로젝트를 못 본다. 같은 이유로 앞서 PR 키
검사도 CI 에서 훅으로 옮겼다.

**더한 줄만 본다.** 파일 전체를 보면 내가 안 건드린 낡은 키가 남을 막고, 지난 엔트리를
안 고치는 저널은 영영 못 건드리게 된다 — 지운 키 서른셋이 저널 40여 파일에 그대로
살아 있다.

## 어떻게 검증했나

- 계획서·규칙 문서의 키 217 종을 조회했다. 없는 키 47 곳 → **0 곳**
- 검사기가 `CY-99999` 에 404, `CY-813` 에 200 을 내는 것을 확인했다
- 이 브랜치 자신에 검사기를 돌려 안 막히는 것을 확인했다
- 훅 자기검증 87 건 통과

## 남은 위험

**"있는데 내용이 다른" 것은 기계가 못 잡는다.** 검사기는 키의 실재만 본다. 제목과
문맥이 맞는지는 사람이 봐야 하고, 이번에는 800번대 스물을 손으로 대조했다.

**다른 저장소는 안 봤다.** `ci-actions` 와 `cy-be` 에도 같은 자리가 있을 수 있다.
