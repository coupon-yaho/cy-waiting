---
id: AIJ-0355
date: 2026-09-21
kind: analyze
plan: [10-scale-out]
phase: 10
jira: CY-975
agent: claude-opus-5
confidence: high
promoted-to:
---

# 예열을 붙였지만 값은 여전히 안 선다

## 무엇을

착수 판정 회차에 트래픽 예열을 붙이고 등록 지표의 기준선 버그를 고쳤다. 둘 다 맞는 고침
인데 **재현성을 안 준다.** 같은 설정의 두 회차가 10 배 갈린다. 그리고 앞선 저널의 결론 둘을
거둔다.

## 왜 (근거)

**예열이 없었다.** 예열 컨테이너는 크레딧이 문턱에 닿기를 기다릴 뿐 요청을 한 건도 안 보낸다.
등록 경로의 JIT 가 식은 채 스파이크를 맞는다.

**기준선이 예열 앞에서 떠 있었다.** 등록 지표에는 쿠폰 구분이 없다. 다른 쿠폰으로 데우면 그
트래픽이 차분에 그대로 섞여, 2만 요청 회차의 등록 건수가 21,808 로 찍혔다. 기준선을 예열
뒤로 옮겨 고쳤다.

**그래도 안 선다.**

| 회차 | 줄에 선 수 | 등록 건수 | 등록 평균 |
|---|---|---|---|
| 예열 없음 | 17,613 | 18,955 | 166.43ms |
| 예열·기준선 정상 (1) | 19,410 | 19,790 | 25.81ms |
| 예열·기준선 정상 (2) | 3,032 | 6,615 | 239.95ms |

**값이 줄에 선 인원과 반대로 움직인다.** 대부분이 줄에 서면 낮고 적게 서면 높다. 지배
변수는 JIT 가 아니라 **큐 모드가 언제 켜지느냐**이고, 그 시점은 도착 램프에 달렸다. 램프는
생성기가 게이트웨이와 코어를 다투는 이 기계에서 회차마다 흔들린다.

## 거두는 것

**"예열이 지배해 13 분의 1 이 된다" 는 틀렸다** (AIJ-0353). 그 비교는 큐 상태가 다른 회차
끼리 견준 것이었다 — 빠른 쪽은 앞 회차가 줄을 채워 둔 상태에서 돌았다. 같은 조건으로 다시
재니 예열은 166ms 를 26ms 로도 240ms 로도 만든다.

**"예열을 붙였더니 유입이 333 으로 떨어졌다" 는 인과가 아니다.** 그 회차의 예열은
`--no-summary` 가 이 k6 판에 없어 **한 번도 안 돌았다.** 종료 코드를 안 봐서 몰랐다. 안 돈
예열을 원인으로 적고 티켓까지 땄다.

## 고려했으나 택하지 않은 것

- **같은 쿠폰으로 예열하기** — 크레딧이 올라간 채 본 회차가 시작해 전원이 통과한다. 줄에 선
  것이 0 이 되고 뒷단 실패가 58.8% 였다
- **본 회차와 같은 크기로 예열하기** — 제어 평면이 흔들려 리더 확인이 실패했다. JIT 는 수천
  번이면 붙으므로 3,000 으로 줄였다
- **예열을 기본으로 켜기** — 재현성을 안 주면서 회차를 늘리기만 한다. 기본은 꺼짐이다

## 확신이 낮은 부분 / 남은 위험

- 큐 모드 전환 시점이 지배 변수라는 것까지가 이 회차들이 지탱하는 결론이다. 그 전환이 왜
  회차마다 다른지는 안 갈랐다
- 네 회차뿐이다. 분포를 말할 표본이 아니다

## 어떻게 검증했는가

같은 설정으로 네 번 돌렸다. 예열 종료 코드를 보게 만든 뒤에야 "예열이 돌았다" 가 사실이
됐다 — 그 전 두 회차는 예열이 없는 회차였다.

`--summary-mode disabled` 로 바꾸고, 99(임계 위반)와 그 밖의 코드를 갈랐다. 99 는 돌았다는
뜻이라 예열을 막을 이유가 아니다.

## 올린 뒤 고친 것

**고친 오염이 한 자리에 그대로 남아 있었다.** 등록 기준선은 예열 뒤로 옮겼는데, **레디스
CPU 를 긁는 프로브는 예열보다 앞에서 떴다.** 판정기는 표본 파일 전체의 최댓값을 봉우리로
집으므로, 예열 봉우리가 본 회차 것보다 높으면 **착수 여부를 예열이 정한다.** 평균도 예열
구간과 가라앉힘 구간의 유휴 표본에 희석되고, 그 평균이 "코어 하나 천장" 의 분모로 들어간다.

예열을 스택 기동 직후로 옮겨 **비우기·기준선·프로브가 전부 그 뒤에 오게** 했다. 그러면
기준선을 두 번 뜰 필요도 없어진다. 생성기 고정 계산도 같이 앞으로 뺐다 — 예열이 본 회차와
다른 조건으로 돌면 데우는 경로가 달라진다.

**"돌았다" 가 "데웠다" 는 아니다.** 종료 코드 99 를 돌았다는 뜻으로 받았는데, 예열이 통과
경로로만 빠지면 등록 경로를 한 번도 안 지나고도 99 로 끝난다. 막으려던 조용한 통과가 한 칸
뒤에 다시 선 셈이다. 요약을 받아 **줄에 선 건수가 0 이 아닌지**를 본다.

**쿠폰 노브가 러너 쪽에서 반쪽이었다.** 시나리오가 `COUPON` 을 읽게 해 놓고 러너는 그 값을
자식 환경에 안 실었다. 예열 자리만 접두 대입이라 우연히 맞았고 본 회차는 시나리오의
기본값으로 돌았다. `export` 로 고쳤다.

**예열 노브에 검사가 없었다.** 이 스크립트는 `-e` 가 없어서 `sleep abc` 는 실패만 하고
넘어간다 — 가라앉힘 없이 본 회차가 시작하는데 산출물에 흔적이 없다. 재는 쿠폰과 같은 값도
막는다. 자기검증에 세 갈래를 넣어 기본 꺼짐인 경로가 조용히 썩지 않게 했다.

**갈래가 넷이었다.** 계획서 표의 둘째 회차 합이 2 만에 1,826 건(9.1%) 모자랐다. 봉투가 안
맞는 202 · 매진 · 5xx · 연결 실패가 세 갈래 어디에도 안 든다. 계수기를 붙이고 표에 합 칸을
세웠다.

**붙이자마자 물었다.** 옮긴 예열로 한 회차를 돌리니 합이 정확히 2 만이 되고 **그 밖이
541 건**으로 섰다. k6 로그를 보니 전부 EOF 다 — 응답을 아예 못 받은 요청이고, 세 갈래만
세는 동안 이 2.7% 는 어느 표에도 안 나타났다. 한 대에서 2 만 VU 를 띄우는 하네스의
천장이라 제품 결함은 아니지만, **그 천장이 회차마다 얼마나 큰지를 볼 창구**가 없었다.

## 다음 사람이 알아야 할 것

**등록 지연 숫자를 인용할 때 그 회차의 줄에 선 인원을 같이 적는다.** 둘이 반대로 움직여서,
인원을 안 적으면 같은 하네스의 두 값이 10 배 갈린 채 한 표에 들어간다.
