# Phase 10 — Scale-out (R4)

> 큐 샤딩과 Redis 클러스터. **착수 전 반드시 측정한다.**
> 키 스킴은 Phase 3에서 이미 확정했으므로 이 페이즈는 그것을 켜는 작업이다.

**선행**: Phase 8 · **상세 수준**: 제목 + 근거 (진입 시 승격)

---

## Goal

> **피크 100K RPS 와 동시 대기 20,000 을 견딘다 (R4).**
> 그리고 샤딩 후에도 순위가 뒤로 가지 않는다.

R4 는 축이 둘이다. **20,000 은 큐에 서 있는 사람 수**(Redis 쪽 한계)이고,
**100K RPS 는 초당 요청 수**(게이트웨이 쪽 한계)다. 20,000 명이 2초마다 폴링해도
10K RPS 라 100K 의 1/10 이다 — 나머지는 통과 트래픽과 신규 진입이다.
**둘을 같이 재지 않으면 R4 의 절반만 증명한 것이다.**

| 통과 수치 | |
|---|---|
| **피크 100K RPS 지속 60초** | 5xx 비율 **< 0.1%** · 게이트웨이 오버헤드 p99 **< 5ms** |
| **게이트웨이 노드 증설 시 처리량** | 노드 수에 **선형에 가깝게** 증가 (효율 ≥ 70%) |
| 20,000 동시에서 Redis 노드별 ops 편차 | **≤ ±20%** |
| `enqueue` p99 | S=1 대비 **개선** |
| 순위 단조성 위반 (샤딩 상태) | **0건** |
| 샤드 간 배수 진행 차이 | **≤ 1틱** |
| Phase 8 전 시나리오 재실행 | **통과** |

**마지막 항목이 핵심이다.** 샤딩은 장애 반경을 바꾸므로 회복력 검증을
처음부터 다시 한다.

---

## 1. 착수 게이트 — 계산이 아니라 측정으로 정한다

계산상으로는 샤딩이 필요하다:

```
enqueue.lua ≈ 8 ops
  ZSCORE(멱등 확인) + TIME + GET(maxscore) + ZADD + SET(maxscore)
  + SET(alive) + GET(admitted) + ZCOUNT(순위)

S=1:  20,000명 / 1초  →  160,000 ops/s가 단일 슬롯 = 단일 노드
      단일 노드 현실 한계 ~80–120K ops/s (Lua는 EVALSHA 오버헤드로 더 낮음)
      선착순 오픈은 1초 균등이 아니라 200ms 스파이크  →  실효 800K ops/s 요구
      →  무너진다

S=16: 샤드당 10,000 ops/s (1초 균등) / 50,000 ops/s (200ms 스파이크)
      스파이크 기준 노드당:  3대 267K · 6대 133K · 8대 100K
      →  스파이크를 지속률로 보면 8대가 있어야 한다
```

> **기준을 섞지 않는다.** 위에서 S=1 을 기각한 근거가 200ms 스파이크인데, S=16 을
> "3대면 된다"로 승인할 때 1초 균등을 쓰면 **사과와 오렌지를 비교하는 것**이다.
> 같은 기준으로 재면 3대는 노드당 267K 로 한계의 2.7배다.
>
> **다만 스파이크는 지속률이 아니다.** 160,000 ops 가 200ms 안에 도착하지만
> Redis 는 큐잉으로 흡수한다 — 노드가 100K ops/s 를 내면 3대에서 약 0.53초에
> 배수된다. 그래서 이건 **처리량 실패가 아니라 지연 상승**이고, 얼마나 상승해도
> 되는지는 `enqueue` p99 기준이 정한다.
>
> **그래서 계산으로 대수를 정하지 않는다.** 아래 측정(10.0.x)이 판정하고,
> 계산은 *"어느 자리가 위험한가"* 를 가리키는 데까지만 쓴다.

> **등록 한 건에 명령이 8개 든다** — 시계 조회, 바닥값 읽기·쓰기, `ZADD`, 순위
> `ZCOUNT` 등. 다만 전부 O(1)/O(log N) 이라 노드 안에서 파이프라인된다.
> 직렬화 지점이 없으므로 이 숫자로 판단하지 않고 아래처럼 측정한다.

**그래도 측정한다.**

| ID | 작업 |
|---|---|
| 10.0.1 | S=1로 20,000 동시 부하 (6.6.4 시나리오, 200ms 램프) |
| 10.0.2 | Redis `INSTANTANEOUS_OPS_PER_SEC` 피크 기록 |
| 10.0.3 | `enqueue` p99 기록 |
| 10.0.4 | `SLOWLOG` — Lua 실행 시간 |
| 10.0.5 | **판정**: 피크 ops 가 단일 노드 한계의 60% 초과 (p99 예산이 정해지면 그것도 조건에 넣는다 — D-L1) |

> **둘 중 하나면 착수한다.** 스파이크는 지속 처리량이 아니라 **지연**으로 먼저
> 드러난다 — ops 가 한계에 안 닿아도 200ms 에 몰린 요청이 큐잉되며 p99 가 튄다.
> ops 만 보면 "여유 있다"고 판단하는데 사용자는 이미 느리다.
>
> **그런데 절대 예산이 없다.** 계획서의 `enqueue` p99 기준은 전부 *"S=1 대비 개선"*
> 이라는 상대값이다 — **S=16 을 할지 정하는 자리에서 "S=1 대비"는 순환**이다.
>
> 절대 예산은 사용자가 큐 진입에서 얼마나 기다려도 되는지에서 나와야 하고,
> 그건 우리가 정할 값이 아니다 → **[90-decisions.md](90-decisions.md) D-L1**.
> 정해지기 전까지 `10.0.5` 는 ops 조건만으로 판정하고, p99 는 **기록만** 한다.

---

## 2. 샤딩의 근거

**직렬화 지점은 없다.** score 가 카운터가 아니라 timestamp 이므로(A-9) 순서를
강제하는 단일 지점이 없고, `ZADD` 는 O(log N) 이라도 노드 안에서 병렬로 돈다.

**그래도 샤딩은 남는다.** 해시태그 때문에 한 쿠폰의 모든 키가 **같은 노드**에
몰리는 건 그대로다 — 클러스터를 아무리 키워도 한 쿠폰은 한 노드다.
바뀐 건 한계의 성격이다.

| | 전 | 후 |
|---|---|---|
| 무엇이 막나 | 단일 카운터의 순서 강제 | 노드 하나의 ops 처리량 |
| 언제 막나 | 동시 등록이 몰리는 순간 | 피크 ops 가 노드 한계에 근접할 때 |
| 착수 판단 | 사실상 필수 | **측정으로 정한다** — 10.0.5 의 60% 조건 유지 |

샤딩 없이 20,000 을 감당할 가능성이 커졌다. **키 스킴은 그대로 유지한다** —
접미사 없는 S==1 형식(E-1)이 있어야 나중에 도입할 수 있다.

---

## 3. 샤드 수는 쿠폰 오픈 시점에 고정한다

**동적으로 S를 바꾸면 sticky가 깨진다.** S=1일 때 접미사 없는 키에 줄 선 사람이
S=16으로 바뀌면 `queue:{cid:7}`을 보게 되고, 아무것도 못 찾아 `NOT_QUEUED`를
반환한다 — **줄 서 있던 사람이 자리를 통째로 잃는다.**

선착순 이벤트는 어느 쿠폰이 핫한지 운영자가 미리 안다.

| 규칙 | 내용 |
|---|---|
| S는 `coupon:policy`에 쿠폰별로 둔다 | 값은 **JSON** `{"mode":"ADAPTIVE","shards":4}` (E-12) |
| 기본값 **1** | 콜드 쿠폰은 현재 동작 그대로, 키도 그대로 |
| **큐가 빈 상태에서만 S 변경 가능** | 코드 가드 + 운영 규칙 |
| 산정 기준 | `S ≈ 예상 최대 동시 대기 / 2,000`, 2의 거듭제곱 |

20,000 동시면 **S=16**.

---

## 4. 샤드별 배수량은 인원 비례로 나눈다

`admit`을 균등 분배하면 **작은 샤드가 먼저 비어 크레딧을 낭비**하고 큰 샤드의
사람은 오래 기다린다. 사용자에게는 "내 순번만 안 줄어든다"로 보인다.

```
admit_s = round(admit × waiting_s / waiting_total)   + 나머지 분배
```

`waiting_s`는 수요 수집에서 이미 샤드별로 모으므로 추가 비용이 0이다.

**편차**: N=20,000, S=16이면 샤드당 평균 1,250, 표준편차
`√(20000 × 1/16 × 15/16) ≈ 34명 (±2.7%)`. 인원 비례 배분이면 배수 속도도
같은 비율로 맞으므로 샤드 간 진행 차이는 1틱 안에 머문다.
**"나보다 늦게 온 사람이 먼저 받았다"는 최대 1초 수준**이다.

---

## 5. 순위 추정 — 근사의 성질이 유리하다

```
내 전역 순위 R̂ = 로컬 순위 × S
```

내 앞의 R-1명이 각각 1/S 확률로 내 샤드에 들어오므로
`A ~ Binomial(R-1, 1/S)`이고, `R̂ = (A+1)×S`의 산포는:

```
sd(R̂) = S × √( (R-1)/S × (1-1/S) ) = √( (R-1)(S-1) )
편향   = S - 1                                    (상수, 무시 가능)
```

정확식 `√((R-1)(S-1))`로 계산 (S=16):

| 진짜 순위 R | 오차(sd) | 상대 오차 | credit=360 기준 ETA 오차 |
|---|---|---|---|
| 20,000 | ±548 | ±2.7% | **±1.5초** (ETA 55.6초) |
| 5,000 | ±274 | ±5.5% | ±0.8초 |
| 1,000 | ±122 | ±12% | ±0.3초 |
| 100 | ±39 | ±39% | ±0.1초 |

**상대 오차는 앞으로 갈수록 커지지만 절대 오차는 작아진다.** 사용자가 체감하는
것은 절대값이므로 이 추정기는 필요한 곳에서 정확하다.

### 5.1 단조성이 정확도보다 중요하다

```
로컬 순위 = ZCOUNT queue:{cid:s} (admitted_s (my_score
admitted_s 는 앞으로만 이동  →  로컬 순위 단조 감소  →  × S 해도 단조 감소  ✓
```

`admitted`를 스냅샷에 넣지 않고 폴링마다 직접 읽는 결정(A-8)이 여기서 값을 한다 —
복제본마다 값이 달랐다면 샤딩 후 순위가 요동쳤을 것이다.

> **timestamp 전환으로 더 정확해질 여지가 생겼다.** 샤드가 갈려도 시계가 하나라
> score 가 서로 비교 가능하다. 샤드별 `ZCOUNT` 를 파이프라인으로 합치면
> **추정이 아니라 정확한 전역 순위**가 나온다. 대가는 폴링당 Redis 호출 S개다.
> 아래 오차표는 `× S` 추정을 유지할 때의 값이고, 정확도가 문제가 되면
> 합산으로 바꾼다.

### 5.2 ETA 오차의 지배항은 샤딩이 아니다

샤딩이 만드는 ETA 오차는 ±1.5초. 반면 `credit`은 매 틱 출렁여 GC 한 번에
ETA가 두 배로 뛸 수 있다. **Phase 2의 EWMA가 훨씬 큰 기여를 한다.**

---

## 6. Task

### 6.1 샤딩 활성화

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.1.1 | `coupon:policy` 를 JSON 으로 (`pcall(cjson.decode, …)`) | 하위 호환 — `shards` 없으면 1, 모르는 필드는 무시, **파싱 실패도 기본값** |
| 10.1.2 | `CouponState.shards` 전파 | 전 게이트웨이가 같은 값 |
| 10.1.3 | **큐가 비지 않으면 S 변경 무시 + 경고** | 순번 유실 방지 |
| 10.1.4 | 데이터 플레인이 샤드 키를 사용 | |
| 10.1.5 | S=1에서 키 문자열이 Phase 3과 동일 | 회귀 |
| 10.1.6 | **큐 등록 상한을 샤드 수로 나눠 넘김** — 내림해서 0 이 되지 않게 한다 | S=4 에서 실효 상한이 4배가 아님 · 나눈 값이 0 이 되어 전원 거절이 되지 않음 |

### 6.2 수요 수집·배분

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.2.1 | 샤드별 ZCARD fan-out | 슬롯 교차 없음 |
| 10.2.2 | `waiting_total` = 샤드 합 | |
| 10.2.3 | `ShardSplitter` 비례 분할 구현 | 나머지 분배 포함 |
| 10.2.4 | `waiting_s == 0`인 샤드 제외 | |
| 10.2.5 | 샤드별 INCRBY fan-out | |
| 10.2.6 | 샤드 간 진행 편차 검증 | 1틱 이내 |

### 6.3 큐 생명주기 샤드 대응

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.3.1 | sweeper를 샤드 단위로 실행 | |
| 10.3.2 | 매진 큐 정리를 전 샤드에 | |
| 10.3.3 | `waiting` 표본 보정을 샤드별로 (D-Q1) | |

### 6.4 순위·ETA

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.4.1 | 순위 추정에 × S 적용 | |
| 10.4.2 | **단조성 속성 테스트** — 샤딩 상태에서 | 위반 0 |
| 10.4.3 | 오차 분포가 이론값과 일치 | 시뮬레이션 |

### 6.5 클러스터 전환

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.5.1 | 클러스터 설정 + 토폴로지 갱신 주기 | |
| 10.5.2 | MOVED/ASK 리다이렉트 처리 | |
| 10.5.3 | 전 Lua를 클러스터 모드에서 실행 | Phase 3 G3.4 재확인 |
| 10.5.4 | `gw:snapshot` 단일 슬롯 부하 확인 | 노드 20대 × 2/s = 40 ops/s |
| 10.5.5 | 노드 장애 시 fail-open 동작 | 카오스 C3 |

### 6.6 규모 검증 — R4 의 나머지 절반

샤딩은 **Redis 쪽 한계**를 푼다. 100K RPS 는 **게이트웨이 쪽 한계**라 별도로 재야 한다.

| ID | 작업 | 주요 테스트 |
|---|---|---|
| 10.7.1 | 100K RPS 부하 시나리오 (`test/load/peak.js`) | 통과·폴링·진입을 실제 비율로 섞는다 |
| 10.7.2 | **100K 지속 60초** 측정 | 5xx < 0.1% · 오버헤드 p99 < 5ms |
| 10.7.3 | **게이트웨이 노드 증설 효율** 측정 | N대 → 2N대에서 처리량 증가율 ≥ 70% |
| 10.7.4 | 병목 지점 계측 (게이트웨이 CPU · Redis ops · 네트워크) | 다음에 무엇을 늘려야 하는지가 나온다 |
| **10.7.5** | **앞단 LB 기준선** — `k6 → LB → 더미 응답` 만으로 한계 RPS 와 p99 를 먼저 잰다 | LB 자체의 오버헤드가 분리된다 |
| **10.7.6** | **10.7.2 결과에서 10.7.5 를 뺀 값**을 게이트웨이 오버헤드로 본다 | 링크·LB 탓을 게이트웨이 탓으로 오인하지 않는다 |

> **부하는 실제 경로대로 LB 를 통과시켜 건다.** 게이트웨이에 직접 때리면 실제
> 요청 구조가 아니다. 대신 **LB 가 측정 대상에 섞인다** — 앞단이 링크·PPS 한계에
> 걸리면 `오버헤드 p99 < 5ms` 가 게이트웨이와 무관한 이유로 깨진다. 그래서 기준선을
> 따로 재고 뺀다 (10.7.5 · 10.7.6). 기준선 없이 미달이 나오면 **누구 탓인지 말할 수
> 없다.**

> **스텁 백엔드로 잰다는 한계를 안고 간다.** 실제 쿠폰 서비스의 GC·커넥션 풀·DB
> 락 특성은 재현되지 않는다. 여기서 재는 것은 **게이트웨이가 100K 를 흘려보낼 수
> 있는가**지 전체 시스템이 100K 를 처리하는가가 아니다.

### 6.7 마이그레이션

| ID | 작업 |
|---|---|
| 10.6.1 | 코드 배포 → 전 쿠폰 S=1, 기존과 동일 동작 확인 |
| 10.6.2 | 단일 Redis 유지한 채 테스트 쿠폰 하나만 S=4로 검증 |
| 10.6.3 | 클러스터 전환 (별도 배포) |
| 10.6.4 | 핫 쿠폰 S=16 적용 |
| 10.6.5 | 운영 규칙 문서화 — **S 상향은 이벤트 시작 전에만** |

---

## 7. Exit Gate

| ID | 기준 | 검증 |
|---|---|---|
| G10.1 | 20,000 동시에서 Redis 노드별 ops 편차 **±20% 이내** | 부하 |
| G10.2 | `enqueue` p99가 S=1 대비 개선 | 부하 |
| G10.3 | **순위 단조성 위반 0** (샤딩 상태) | 10.4.2 |
| G10.12 | **틱 지연 p99 < 100ms — 20,000 동시에서** (Phase 4 G4.3 의 실규모 재확인) | 부하 |
| G10.4 | 샤드 간 배수 진행 차이 **≤ 1틱** | 10.2.6 |
| G10.5 | 순위 추정 오차가 이론 분포 안 | 10.4.3 |
| G10.6 | 큐가 있는 상태에서 S 변경 시도 → 거부 + 경고 | 10.1.3 |
| G10.7 | 전 Lua가 클러스터에서 오류 없이 실행 | 10.5.3 |
| G10.8 | 클러스터 노드 1대 장애 시 **해당 샤드만 영향** | 카오스 C3 |
| **G10.10** | **피크 100K RPS 를 60초 지속** — 5xx < 0.1%, 오버헤드 p99 < 5ms (**LB 기준선을 뺀 값**) | 10.7.2 · 10.7.6 |
| **G10.13** | **앞단 LB 기준선이 목표 RPS 를 감당한다** — 못 하면 게이트웨이 수치를 판정할 수 없다 | 10.7.5 |
| **G10.11** | 게이트웨이 노드 증설 시 처리량이 **선형에 가깝게** 증가 (효율 ≥ 70%) | 10.7.3 |
| G10.9 | **Phase 8 전 시나리오 재실행 통과** | 카오스 회귀 |

**G9.9가 핵심이다.** 샤딩은 장애 반경을 바꾸므로 회복력 검증을 처음부터 다시 한다.

### 게이트 커밋

```
chore(gate): Phase 10 게이트 통과

20k 스파이크: 노드별 ops 편차 14% / enqueue p99 41ms (S=1 에서는 불가)
순위 단조성 10만 시퀀스 위반 0
샤드 배수 편차 1틱 이내 / 카오스 12개 재실행 통과

Refs: CY-<phase-10-gate>
```

---

## 8. 롤백

**S 하향은 진행 중인 큐를 깨뜨린다.** 이벤트 종료 후에만 가능하다.
클러스터 전환 자체의 롤백은 데이터 이관이 필요하므로 사실상 단방향이다 —
9.6.2의 단일 Redis 검증을 충분히 하고 넘어간다.

---

## 지라 티켓

티켓에는 라벨을 붙이지 않는다 — 보드에서 방해만 된다. 계획서 ID 와의 대응은 [91-jira-map.md](91-jira-map.md) 9절에 있다.

**에픽** [`CY-26`](https://shseol.atlassian.net/browse/CY-26) 규모 확장

기능 티켓은 아직 발번하지 않았다. 이 페이즈에 진입할 때 만든다.

브랜치는 티켓 단위로 판다. 커밋 푸터는 지라 키 하나만 남긴다 — 계획서 ID 를 박지 않는다.

```
Refs: CY-42
```
