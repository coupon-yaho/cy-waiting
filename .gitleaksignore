# 여기 적은 것은 **지나간 커밋의 지문**이다. 지금 파일은 이미 고쳤지만
# 검사는 커밋 범위를 훑으므로 옛 커밋이 계속 걸린다. 이력을 다시 쓰지 않는다.
#
# 지문은 커밋 하나를 정확히 가리킨다. 파일이나 규칙을 통째로 여는 것이 아니라,
# 그 커밋의 그 줄만 넘긴다 — 같은 파일에 새 값이 들어오면 그대로 잡힌다.
#
# 둘 다 시험용 키다. 이후 커밋에서 not-a-real-secret 표식을 붙였다.
ae40c4fff3676897fd50b12106a11afcb296a448:src/test/resources/application-test.yml:generic-api-key:16
ea309db98d1c58a45a12aab8c381e56e27b273f7:src/test/java/com/kafkick/waiting/domain/queue/QueueTokenTest.java:generic-api-key:20
