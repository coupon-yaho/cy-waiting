# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# 자격 증명은 저장소에 들어오지 않는다
.env
.env.*
*.token

# 락파일은 스캔 직전에 만든다 — Trivy 가 Gradle 을 이것으로만 읽어서지
# 잠금 자체가 목적이 아니다. 커밋하면 갱신 부담이 계속 따라온다 (CY-293).
gradle.lockfile

# 작업 로그 색인은 생성물이다 — 손으로 유지하면 브랜치마다 같은 자리에서
# 충돌하고, 그 값은 이미 저널 프론트매터에 다 있다 (CY-303).
ai/journal/index.md

# 에이전트 리뷰 증거는 로컬 상태다 — 커밋하면 남의 리뷰가 내 증거가 된다.
.claude/.agents-reviewed

# 부하 실행 산출물. 저장소에 두면 옛 결과를 새 판정에 쓴다.
k6-summary.json
